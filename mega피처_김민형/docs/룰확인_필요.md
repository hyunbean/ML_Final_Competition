# ⚠️ 호스트 룰 확인 필요 — 외부지식 피처 (external block)

> `block_s1_ext`(외부 상품-타깃-성별, +0.00118) 사용 가부가 여기 달림. **확인 전까지 보류.**

## 호스트에 물을 질문 (그대로 복사)
> "과제가 '주어진 데이터로부터 가장 우수한 단일 모델'인데:
> 1. **사전학습 모델**(예: 한국어 BERT/SBERT)로 상품명 임베딩을 피처로 쓰는 것 — 허용되나요?
> 2. **외부 세계지식/사전**(예: 브랜드·카테고리의 일반적 타깃 성별을 LLM/공개정보로 부여)으로 피처를 만드는 것 — 허용되나요?
> 3. 외부 **데이터셋·고객 레코드** 추가는 (당연히) 금지로 이해하면 되나요?"

## 우리 입장 정리
- **1번(사전학습 LM)**: 팀이 이미 BERT/RoBERTa/SBERT 임베딩 사용 중 → 사실상 같은 범주.
- **2번(외부지식 피처)** = `block_s1_ext`의 근거. 1번이 OK면 2번도 OK일 가능성 높음(둘 다 외부 사전지식, 외부 행 추가 아님).
- **3번**: 외부 고객데이터는 우리도 안 함(join 키도 없음).

## 결과별 행동
- **허용** → `block_s1_ext` 사용. (단 cross와 stack 안 되니 **둘 중 하나만**; external 근소우위 +0.00118)
- **불허** → `block_s1_ext` 폐기, **`block_s1_cross`(내부, +0.0011)로 대체** — 같은 "선물 갭" 신호를 내부데이터만으로 잡으므로 룰 안전. 사실상 손실 거의 없음.

→ **즉 룰이 어떻든 "선물 갭 신호 +0.0011~0.0012"는 확보** (external 허용시 약간 우위). 리스크 헤지 완료.
