# 데이터(대용량) — git에 올리지 않음. 각 머신에 따로 배치하고 KML_DATA로 경로 지정.
*.csv
data/

# 체크포인트(임시) / 제출 파일
artifacts/checkpoints/
artifacts/submissions/

# 대용량 바이너리(임베딩/모델 캐시)
*.model
*.bin
*.pkl

# 대용량 미디어(1등솔루션 녹화파일 등) — GitHub 100MB 제한
*.mp4
*.mov
*.avi

# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# 개인 개발환경 설정(로컬 절대경로 포함 — 커밋 금지)
.claude/

# ↑ artifacts/*.npy (folds) 와 artifacts/oof/*.npy, *.json 은 추적됨(팀 공유 대상)

# OneDrive 임시
.tmp.driveupload/

# optuna study
*.db

# AutoGluon 모델 저장(대용량)
artifacts/ag_fold*/

# 전송용 압축본(전달 후 불필요)
*.zip

# 김민형 공유 작업폴더(중복 npy 300+) / 타팀·교수 자료(저작권)
김민형_0610/
kml2026s_leaderboard analysis.html
1등팀_*.html
3등팀_*.pdf
